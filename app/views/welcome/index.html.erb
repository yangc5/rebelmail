<h1>Welcome to rebel mail</h1>
<span>Enter a message and send emails to following users:</span>
<ul>
<% @users.each do |user|%>
<li><%= user.name + ': ' + user.email%></li>
<% end %>
</ul>
<%= form_tag(welcome_send_email_path) do %>
<%= text_area_tag 'Message', "", :size=>"50x20"%>
<%= submit_tag 'Send Emails'%>
<% end %>
